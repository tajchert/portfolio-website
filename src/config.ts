// Site-wide config. Single source of truth for values that change over time.

// The blog currently lives on Hashnode's free subdomain. When the blog moves
// to self-hosting (e.g. blog.mtajchert.com), change this one value — every
// link on the site (nav, Connect card, structured data) updates with it.
export const BLOG_URL = 'https://mtajchert.hashnode.dev/';
